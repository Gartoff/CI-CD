name: 01 - Integration

on:
  push: 
    branches:
      - develop
jobs: 
  Test_Back_Front:
    uses: ./github/workflows/01-1_TestBackFront.yml
  
    # Quality_Sonarqube:
    #   needs: [Test_Back_Front]
    #   uses: ./.github/worflows/_02_Sonarqube.yml

    # Security_Snyk:
    #   needs: [Quality_Sonarqube]
    #   uses: ./.github/worflows/_03_Snyk.yml
      