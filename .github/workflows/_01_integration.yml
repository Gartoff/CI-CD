name: 01 - Integration

on:
  push: 
    branches:
      - Develop
      

jobs: 
  Test_Back_Front:
    uses: ./.github/workflows/01-1_TestBackFront.yml
  
  Quality_Sonarqube:
    needs: [Test_Back_Front]
    uses: ./.github/worflows/01-2_Sonarqube.yml

    # Security_Snyk:
    #   needs: [Quality_Sonarqube]
    #   uses: ./.github/worflows/_03_Snyk.yml
      